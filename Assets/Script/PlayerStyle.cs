using UnityEngine;

public class PlayerState : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    private float agreessionScore;
    // private float
    
    
    
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

         
    }

    
}
